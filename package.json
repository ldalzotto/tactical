{
  "name": "tactical",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cmake -S . -B build && cmake --build build",
    "serve": "node server.js",
    "start": "npm run serve"
  }
}
