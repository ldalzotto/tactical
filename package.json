{
  "name": "tactical",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cmake -S . -B build && cmake --build build",
    "start": "node server.js",
    "test": "node --test"
  }
}
